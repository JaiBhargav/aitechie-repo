
// Add any interactivity or dynamic behavior for the landing page here
// Example: Display a welcome message in the console
console.log("Welcome to AI Techie!");

// Example: Add event listeners for buttons (if needed)
document.querySelectorAll(".cta-buttons button").forEach(button => {
    button.addEventListener("click", () => {
        console.log(`Button clicked: ${button.textContent}`);
    });
});